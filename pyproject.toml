[project]
name = "phc-supply-resilience"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "ortools",
    "google-generativeai",
]

[tool.vercel]
entrypoint = "backend.main:app"
